function solution(my_string, k) {
    var answer = '';
    for(let i = 0; i < k; i ++){
        answer += my_string
    }
    console.log(answer)
    return answer;
}
//for문을 k만큼 단순적으로 반복시키면 되는 문제입니다.